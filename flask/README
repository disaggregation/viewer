
                        / Disaggregation Data Viewer/

    ~ What is disaggregation?

      A SQLite and Flask (python web based) disaggregation data displayer 

    ~ How do I use it?

      1. edit the configuration in the disaggregation.py file or
         export an disaggregation_SETTINGS environment variable
         pointing to a configuration file.

      2. install the app from the root of the project directory

         pip install --editable .

      3. tell flask about the right application:

         export FLASK_APP=disaggregation

      4. fire up a shell and run this:

         flask initdb

      5. now you can run disaggregation:

		 flask run --host=0.0.0.0

         the application will greet you on
         http://YourIP:5000/

    ~ Is it tested?

      not jet.....  Run the `python setup.py test` file to
      see the tests pass.
